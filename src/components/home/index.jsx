import React from "react";
import { useAuth } from "../../contexts/authContext";

const Home = () => {
  return (
    <div className="primary-height mx-6">
      <p>Home Page</p>
    </div>
  );
};

export default Home;
